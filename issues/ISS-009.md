# ISS-009: Publish version 0.4.0

## Metadata
- Type: chore
- Status: closed
- Priority: 1
- Labels: release, breaking-api
- Assignee: codex
- Created: 2026-07-13
- Updated: 2026-07-13
- External ref: none

## Description
Publish the security and correctness fixes, including the breaking checked Writer API, as the next pre-1.0 minor release. Version 0.3.0 does not contain the current parse-error span fix or the new hardening work.

## Design
After every fix issue is closed, run formatting, generated-interface, check, and test gates on all targets; bump `moon.mod` to 0.4.0; publish; then commit the release metadata and close the epic.

## Acceptance Criteria
- [x] All prerequisite fix issues are closed.
- [x] Full validation passes with no generated or formatting drift.
- [x] `moon.mod` declares 0.4.0 and `moon publish` succeeds.
- [x] The release change and closed tracker state are committed.

## Relationships
- Depends on: ISS-002, ISS-003, ISS-004, ISS-005, ISS-006, ISS-007, ISS-008
- Parent: ISS-001
- Related: none
- Discovered from: none

## Notes
- 2026-07-13: Planned as a minor release because Writer validation intentionally breaks the unsafe API.
- 2026-07-13: Claimed after both review axes confirmed that all fix findings are resolved.

## Close Notes
- Set `moon.mod` to 0.4.0 after all fix issues and both review axes were clear.
- Verified `moon fmt`, `moon info --target all`, strict all-target checking, `moon test --target all` (845/845 on four backends), `git diff --check`, and the 100 KiB package contents.
- `moon publish --dry-run` validated the extracted package and returned `202 Accepted`; the CLI misclassified that successful dry-run status as an error.
- `moon publish` revalidated the extracted package and completed with `Server status: 200 OK`.
