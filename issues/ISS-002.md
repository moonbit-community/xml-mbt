# ISS-002: Disable implicit external entity file access

## Metadata
- Type: bug
- Status: closed
- Priority: 0
- Labels: parser, security, xxe
- Assignee: codex
- Created: 2026-07-13
- Updated: 2026-07-13
- External ref: none

## Description
`Reader::from_file` implicitly resolves `SYSTEM` entities relative to the input file and permits paths to escape that directory. Parsing untrusted XML can therefore disclose arbitrary readable local files.

## Design
Remove automatic filesystem resolution and its stored base path. External entity declarations remain syntactically parseable, but references fail as undefined unless a future explicit resolver API is designed. Do not retain a compatibility switch for the unsafe behavior.

## Acceptance Criteria
- [x] Parsing a file cannot read a referenced local file through a `SYSTEM` entity.
- [x] Existing internal entity behavior continues to work.
- [x] Public documentation no longer promises implicit external entity resolution.

## Relationships
- Depends on: none
- Parent: ISS-001
- Related: ISS-003
- Discovered from: none

## Notes
- 2026-07-13: Reproduced with `SYSTEM "../secret.txt"`, which exposed content outside the document directory.
- 2026-07-13: Claimed for test-driven implementation.

## Close Notes
- Removed Reader's base-path state and all implicit filesystem access from external entity declarations.
- Added a real-file regression test proving `SYSTEM` references remain unresolved.
- Verified with `moon check --target all --warn-list +73 --deny-warn`, `moon info --target all`, and `moon test --target all` (818/818 on wasm, wasm-gc, js, and native).
