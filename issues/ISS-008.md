# ISS-008: Hide the conformance adapter from the public API

## Metadata
- Type: chore
- Status: closed
- Priority: 2
- Labels: api, conformance, cleanup
- Assignee: codex
- Created: 2026-07-13
- Updated: 2026-07-13
- External ref: none

## Description
`to_libxml_format` is a conformance-test adapter but is declared public, appears in the generated package interface, and is absent from the planned API in `spec.mbt`.

## Design
Make the adapter package-private. Same-package conformance tests can continue using it without exposing a test-specific representation to library users.

## Acceptance Criteria
- [x] `to_libxml_format` is absent from the generated public interface.
- [x] Conformance tests still pass.
- [x] No other code is changed beyond fallout from hiding this symbol.

## Relationships
- Depends on: none
- Parent: ISS-001
- Related: none
- Discovered from: none

## Notes
- 2026-07-13: The function appears in `src/pkg.generated.mbti` but not `src/spec.mbt`.
- 2026-07-13: Claimed; first validating whether current conformance tests already have package-private access.

## Close Notes
- Moved the lxml-format adapter and its private helpers out of production sources into a white-box conformance-test helper.
- Renamed the two conformance suites to white-box test files and updated the generator output path so regeneration preserves that boundary.
- Regenerated `pkg.generated.mbti` and confirmed `to_libxml_format` is absent.
- Verified with `moon check --target all --warn-list +73 --deny-warn`, `moon info --target all`, and `moon test --target all` (842/842 on wasm, wasm-gc, js, and native).
